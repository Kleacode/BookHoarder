tag_info:
  type: object
  properties:
    name:
      type: string
    userId:
      type: integer
      format: int64

exist_tag:
  allOf:
    - $ref: "#/tag_info"
    - properties:
        tagId:
          type: integer
          format: int64