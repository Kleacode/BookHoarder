tag_info:
  type: object
  properties:
    name:
      type: string

exist_tag:
  allOf:
    - $ref: "#/tag_info"
    - properties:
        tagId:
          type: integer
        userId:
          type: integer